@using System.Reflection;

@{
    ViewData["Title"] = (Model.Rule != null)?"Reports for rule " + Model.Rule.ID : "Reports";
}

@model DetailedReportViewModel

@if (Model == null)
{
    <h1>No data found.</h1>
}
else
{
    var props = Model.Rule.GetType().GetProperties();
}